/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['https://salespulse-api.vercel.app/api'], // Remplacez par le domaine de votre serveur
      },

}

module.exports = nextConfig
